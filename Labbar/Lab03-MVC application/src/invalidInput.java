//Handles our invalidInput, delegated work from Controller.
public class invalidInput{
    public static boolean isNumeric(String str) {
        try {
            Integer.parseInt(str);
            return true;
        } catch (NumberFormatException e) {
            return false;
        }
    }
}
